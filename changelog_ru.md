это временный CHANGELOG, изменения отсюда нужно грамотно добавить в настоящий CHANGELOG.md, указать какие изменения
breaking changes, разбить по группам, расписать все грамотно, профессионально, идиоматично, с соблюдением лучших практик
разработки, составления и ведения технической документации, проверить по кодовой базе все ли изменения внесены, может
есть что-то измененное в кодовой базе, что пользователь забыл здесь указать.

## что я сделал (изменения которые я, то есть пользователь, документирую)

- добавил workflows для создания релизов и файл FUNDING.yml, но не знаю, стоит ли об этом говорить в CHANGELOG.md
- в fonts добавил шрифт "JetBrains Mono NerdFont Mono"
- обновил .gitignore добавил .editorconfig, тоже наверно об этом не стоит в CHANGELOG.md, не знаю
- в .zfunc удалил функцию fn_new_libreoffice_doc и шаблон который она копирует, так как больше не пользуюсь LibreOffice
- в configs
  - удалил .htaccess, так как он больше нигде не используется, устарел Apache сервер не использую его больше
  - удалил .npmrc, за ненадобностью, излишняя настойка
  - в .zshrc много что поменял, например из калечащего визуальный редактор теперь zeditor, многие переменные другие, 
    алиасы тоже, убраны алиасы для yay, добавлены для управления vpn, TechWriter смотри сам diff с предыдущим комитом и 
    текущим состоянием что поменялось 
  - удалил файлы git.plugin.zsh и web-search.plugin.zsh потому что не использую больше эти плагины, не поддерживаю 
    больше их
  - в 99-custom.conf убрал параметры vm.dirty_ratio и vm.dirty_background_ratio, они были слишком агрессивные, 
    значения по умолчанию в системе итак оптимальны  
  - в файле grub добавлена новая тема arch-silence-grub-theme-git для меню grub, так же изменены параметры в 
    переменной GRUB_CMDLINE_LINUX_DEFAULT, предыдущие оптимизации были слишком агрессивны и не давали лучшего 
    стабильного результата, изменен параметр GRUB_GFXMODE, надо сказать что если у вас не поддерживается разрешение 
    1920x1080 то поменяйте на своё подходящее, либо поставьте значение auto
  - в mkinitcpio.conf добавлен модуль btrfs в MODULES, это нужно только если у вас используется файловая система btrfs
  - добавил свои конфиги .gitconfig для git и .xinitrc для старта сессии xfce4
- в templates
  - обновил .dockerignore, .gitignore 
- добавлен каталог custompkgs в котором 2 драйвера для видеокарты Nvidia, в официальных репозиториях драйвера 
  nvidia-dkms больше не существуют, их заменили на nvidia-open-dkms которые не поддерживают старые видеокарты, 
  например GTX 1080, драйвера в папке custompkgs это последняя версия из серии драйверов поддерживающих старые 
  видеокарты, их можно установить из этих локальных пакетов с помощью `sudo pacman -U nvidia-dkms-580.95.
  05-1-x86_64.pkg.tar.zst nvidia-utils-580.95.05-1-x86_64.pkg.tar.zst` либо по ссылке если там они ещё доступны `sudo pacman -U https://archive.archlinux.org/packages/n/nvidia-dkms/nvidia-dkms-580.95.05-1-x86_64.pkg.tar.zst https://archive.archlinux.org/packages/n/nvidia-utils/nvidia-utils-580.95.05-1-x86_64.pkg.tar.zst`
